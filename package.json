{
  "name": "cozyswap-dex",
  "version": "1.0.0",
  "description": "CozySwap DEX on Plasma Chain",
  "scripts": {
    "build": "echo 'Building CozySwap...' && mkdir -p dist && cp index.html dist/ && echo 'Build complete!'"
  },
  "dependencies": {
    "ethers": "^5.7.1"
  }
}
